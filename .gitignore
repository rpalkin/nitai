# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Qdrant local data
/db/

# Environment / secrets
.env
.env.local
.env.*.local
**/.env
deploy/overlays/local/secret.env

# direnv — local shell environment loader
.envrc

# Generated protobuf Go code (go.mod kept so module resolution works without make proto)
gen/go/**
!gen/go/go.mod

# PostgreSQL data volume
pgdata/

# Restate local state
restate-data/

/*/out/
specs/CURRENT.md
specs/REVIEW.md

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# OpenCode plugin dependencies
.opencode/node_modules/
.opencode/bun.lock

# Go vendor directories (populated via `make vendor`)
api-server/vendor/
go-services/vendor/
e2e/vendor/
lib/vendor/
